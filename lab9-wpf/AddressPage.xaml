<Page x:Class="MultiStepFormWPF.AddressPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Адрес">
    <Page.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="38" />
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="BorderBrush" Value="#C7D0DB" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="MinWidth" Value="118" />
            <Setter Property="Height" Value="38" />
            <Setter Property="Margin" Value="10,0,0,0" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#2F80ED" />
            <Setter Property="BorderBrush" Value="#2F80ED" />
        </Style>
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Foreground" Value="#1F2937" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#C7D0DB" />
        </Style>
    </Page.Resources>

    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,18">
            <TextBlock FontSize="20" FontWeight="SemiBold" Foreground="#1F2937" Text="Шаг 3 из 3: адрес" />
            <TextBlock Margin="0,6,0,0" FontSize="14" Foreground="#4B5563" Text="Заполните город, улицу и номер дома." />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                <TextBlock Margin="0,0,0,6" FontWeight="SemiBold" Foreground="#1F2937" Text="Город" />
                <TextBox x:Name="CityTextBox" />
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="0,0,0,12">
                <TextBlock Margin="0,0,0,6" FontWeight="SemiBold" Foreground="#1F2937" Text="Улица" />
                <TextBox x:Name="StreetTextBox" />
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="0,0,0,12">
                <TextBlock Margin="0,0,0,6" FontWeight="SemiBold" Foreground="#1F2937" Text="Номер дома" />
                <TextBox x:Name="HouseNumberTextBox" />
            </StackPanel>

            <StackPanel Grid.Row="3">
                <TextBlock Margin="0,0,0,6" FontWeight="SemiBold" Foreground="#1F2937" Text="Номер квартиры (опционально)" />
                <TextBox x:Name="ApartmentNumberTextBox" />
            </StackPanel>
        </Grid>

        <TextBlock x:Name="ErrorTextBlock"
                   Grid.Row="2"
                   Margin="0,14,0,0"
                   FontSize="13"
                   Foreground="#D64545"
                   TextWrapping="Wrap" />

        <StackPanel Grid.Row="3"
                    Margin="0,16,0,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Content="Назад"
                    Click="BackButton_Click"
                    Style="{StaticResource SecondaryButton}" />
            <Button Content="Отправить"
                    Click="SubmitButton_Click"
                    Style="{StaticResource PrimaryButton}" />
        </StackPanel>
    </Grid>
</Page>
