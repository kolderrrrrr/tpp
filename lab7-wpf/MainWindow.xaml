<Window x:Class="CustomButtonTemplateWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Пользовательский шаблон кнопки" Height="560" Width="760"
        MinHeight="500" MinWidth="660"
        Background="#F4F6F8"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <SolidColorBrush x:Key="NormalBackgroundBrush" Color="#2F80ED" />
        <SolidColorBrush x:Key="HoverBackgroundBrush" Color="#1F6FD0" />
        <SolidColorBrush x:Key="PressedBackgroundBrush" Color="#1559AD" />
        <SolidColorBrush x:Key="DisabledBackgroundBrush" Color="#B8C4D1" />

        <Style x:Key="TemplateButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="150" />
            <Setter Property="Height" Value="46" />
            <Setter Property="Margin" Value="0,0,12,12" />
            <Setter Property="Padding" Value="18,0" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="{StaticResource NormalBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="#1359A8" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="RootBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7"
                                RenderTransformOrigin="0.5,0.5"
                                SnapsToDevicePixels="True">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1" />
                            </Border.RenderTransform>

                            <Grid ClipToBounds="True">
                                <Border x:Name="TopGlow"
                                        Height="20"
                                        VerticalAlignment="Top"
                                        CornerRadius="7,7,0,0"
                                        Background="#35FFFFFF"
                                        Opacity="0.45" />
                                <TextBlock x:Name="ButtonContent"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Margin="{TemplateBinding Padding}"
                                           Foreground="{TemplateBinding Foreground}"
                                           Text="{TemplateBinding Content}" />
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{StaticResource HoverBackgroundBrush}" />
                                <Setter TargetName="TopGlow" Property="Opacity" Value="0.7" />
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="RootBorder"
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                             To="1.04"
                                                             Duration="0:0:0.15" />
                                            <DoubleAnimation Storyboard.TargetName="RootBorder"
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                             To="1.04"
                                                             Duration="0:0:0.15" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="RootBorder"
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                             To="1"
                                                             Duration="0:0:0.15" />
                                            <DoubleAnimation Storyboard.TargetName="RootBorder"
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                             To="1"
                                                             Duration="0:0:0.15" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{StaticResource PressedBackgroundBrush}" />
                                <Setter TargetName="TopGlow" Property="Opacity" Value="0.2" />
                                <Setter TargetName="RootBorder" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.97" ScaleY="0.97" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="RootBorder" Property="Background" Value="{StaticResource DisabledBackgroundBrush}" />
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="#99A6B5" />
                                <Setter Property="Foreground" Value="#EEF2F7" />
                                <Setter TargetName="TopGlow" Property="Opacity" Value="0.15" />
                                <Setter TargetName="RootBorder" Property="Opacity" Value="0.75" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButtonStyle" TargetType="Button" BasedOn="{StaticResource TemplateButtonStyle}">
            <Setter Property="Background" Value="#1F8A4C" />
            <Setter Property="BorderBrush" Value="#176B3A" />
        </Style>
    </Window.Resources>

    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="0,0,0,16"
                   FontSize="25"
                   FontWeight="SemiBold"
                   Foreground="#1F2937"
                   Text="Демонстрация пользовательского шаблона Button" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Padding="18"
                    Background="White"
                    BorderBrush="#D7DEE8"
                    BorderThickness="1">
                <StackPanel>
                    <TextBlock Margin="0,0,0,14"
                               FontSize="17"
                               FontWeight="SemiBold"
                               Foreground="#1F2937"
                               Text="Кнопки с одним ControlTemplate" />

                    <Button Content="Сохранить"
                            Style="{StaticResource TemplateButtonStyle}"
                            Click="ActionButton_Click" />
                    <Button Content="Отправить"
                            Style="{StaticResource AccentButtonStyle}"
                            Click="ActionButton_Click" />
                    <Button Content="Предпросмотр"
                            Style="{StaticResource TemplateButtonStyle}"
                            Click="ActionButton_Click" />
                    <Button Content="Недоступно"
                            Style="{StaticResource TemplateButtonStyle}"
                            IsEnabled="False" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2"
                    Padding="18"
                    Background="White"
                    BorderBrush="#D7DEE8"
                    BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Margin="0,0,0,14"
                               FontSize="17"
                               FontWeight="SemiBold"
                               Foreground="#1F2937"
                               Text="Журнал действий" />

                    <ListBox x:Name="ActionListBox"
                             Grid.Row="1"
                             FontSize="14"
                             Background="#F8FAFC"
                             BorderBrush="#D7DEE8"
                             BorderThickness="1" />
                </Grid>
            </Border>
        </Grid>

        <TextBlock x:Name="StatusTextBlock"
                   Grid.Row="2"
                   Margin="0,14,0,0"
                   FontSize="13"
                   Foreground="#4B5563"
                   Text="Готово." />
    </Grid>
</Window>
